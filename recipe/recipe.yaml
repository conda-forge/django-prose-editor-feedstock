schema_version: 1

context:
  name: django-prose-editor
  version: "0.18.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/django_prose_editor-${{ version }}.tar.gz
  sha256: e0b0e92e4d3195a7beb4b495cb44e0f902e8fb11943d2b223911b410d20c67da

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - django >=4.2
    - django-js-asset >=3.1.2

tests:
  - python:
      imports:
        - django_prose_editor
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Prose editor for the Django admin based on ProseMirror
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/matthiask/django-prose-editor/

extra:
  recipe-maintainers:
    - thewchan
